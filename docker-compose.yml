services:
  client:
    build:
      context: ./client
      args:
      - PUBLIC_URL=/client
    networks:
      - test

  web:
      build:
        context: ./web
        args:
        - PUBLIC_URL=/
      networks:
        - test

  nginx:
    container_name: nginx
    build:
      context: ./nginx
    ports:
      - 80:80
    networks:
      - test
    depends_on:
      - web
      - client

networks:
  test: